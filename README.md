# init
```bash
cp env-example .env
yarn init
yanr start
yarn build
```
## build with firebase
```bash
firebase login

firebase init

yarn build
firebase deploy
```

# note: when firebase init ask your public folder you type build folder.